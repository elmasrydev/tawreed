/*
 * Alpine ships with Livewire, which the base layout loads on every page.
 * Interactive pieces (modals, drawers, toasts, the mobile menu) are declared
 * inline with Alpine directives in their Blade components.
 */
